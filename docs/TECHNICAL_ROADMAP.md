# 🗺️ AquaTech Technical Roadmap & System Audit (Архитектура, Бэкенд, Лаунчер, Инфраструктура)

> [!NOTE]
> Документ содержит результаты глубокого технического аудита кодовой базы AquaTech (Minecraft 1.20.1 Forge + Mohist, Cloudflare Workers D1, Python Launcher). Исключены вопросы дизайна/визуала.

---

## 1. ⚙️ Игровой баланс и Серверная сборка (Forge + Mohist)

### 🔴 Критические проблемы и уязвимости:
1. **Уязвимость дюпов и рассинхронизации инвентаря на Mohist**:
   - **Суть**: Mohist совмещает Forge API и Bukkit API. Во время быстрого перетаскивания предметов в верстаках 9×9 Extended Crafting или при выгрузке чанков с работающей ME-сетью AE2 возникают гонки потоков и дублирование предметов.
   - **План исправления**: Реализовать в `aquatech_ui` подписку на `ContainerTransactionEvent`, установить жесткий лимит ввода/вывода в `AutoFisherBlockEntity` (макс. 64 предмета/тик), принудительно синхронизировать `BlockEntity` при выгрузке чанков.

2. **Обход прочности удочек StarCatcher (`RodDurability`)**:
   - **Суть**: Чары `Mending` (Починка) и `Unbreaking` (Прочность) позволяют восстанавливать прочность удочек прямо во время рыбалки, сводя лимиты уловов к нулю.
   - **План исправления**: Перехватывать `AnvilUpdateEvent` и `PlayerXpEvent.PickupXp` для удочек с тегом `aqRodTier` в `aquatech_ui`, блокируя починку Починкой и настраивая фиксированный износ (1 прочность = 1 улов) независимо от чар.

3. **Изолированность экономик (CasesMod Dubloons vs Lightman's Currency)**:
   - **Суть**: Валюта F4 в `casesmod` (дублоны) не связана с физическими монетами `Lightman's Currency`. Игрок не может потратить заработанные от продажи рыбы деньги в магазинах других игроков.
   - **План исправления**: Разработать модуль `CurrencyBridgeHandler` в `com.casesmod` для двустороннего обмена физических монет на виртуальные дублоны аккаунта через GUI Скупщика Рыбы.

### 🟡 Эндгейм и механики:
1. **Контентный тупик после Акта 6 (Quantum Era)**:
   - **Суть**: Пассивный улов через `auto_fisher` после создания Альфа-удочки обесценивает ресурсы.
   - **План реализации**: 
     - **Ocean Prestige**: Сброс прогресса удочек в обмен на перманентные глобальные пассивки.
     - **Расход энергии/ресурсов**: Содержание подводных баз и защитных полей плота за FE/т.
     - **Босс Бездны**: Кастомное событие вызова подводного босса с требованием аквалангов.

2. **Авто-приват островов (SkyblockBuilder + WorldGuard)**:
   - **Суть**: Новые острова Y190 не всегда приватно защищены от соигроков.
   - **План реализации**: Авто-создание региона `/rg claim island_<uuid>` при спавне с блокировкой доступа к блочным сущностям `aquatech_ui`.

---

## 2. 🌐 Сайт, Cloudflare Worker, D1 БД и API (Web Portal & Backend)

### 🔴 Критические проблемы:
1. **Отсутствие реальной оплаты (Store Payment Gateway)**:
   - **Суть**: Эндпоинт `/api/purchase` заблокирован заглушкой `purchasesDisabled()`.
   - **План реализации**: Подключить платежный агрегатор (YooKassa / Lava / Telegram Pay / Cryptomus) через Webhook-эндпоинт `/api/purchase/callback` с валидацией подписи HMAC-SHA256.

2. **Отсутствие шины доставки покупок в игру (Web-to-Game Delivery)**:
   - **Суть**: База D1 изолирована от сервера Mohist.
   - **План реализации**: Создать таблицу D1 `pending_commands` и Worker-эндпоинт `/api/internal/pending-commands`. Добавить модуль в `casesmod` / RCON для регулярного выполнения команд на сервере.

3. **Отсутствие восстановления пароля и поля Email**:
   - **Суть**: В таблице `users` отсутствует `email`. Забытый пароль не подлежит сбросу.
   - **План реализации**: Добавить миграцию D1 `0005_email_reset.sql`, добавить поле `email`, внедрить отправку кодов сброса пароля (Mailgun / Resend API) и эндпоинт `/api/auth/reset-password`.

### 🟡 Расширения портала:
1. **Онлайн-рулетка кейсов (Web Drop Vault)**:
   - **Суть**: Кейсы открываются только в игре F4.
   - **План реализации**: Реализовать открытие кейсов на сайте с сохранением наград в таблицу `player_vault` в D1 и последующим забором командой `/casesmod claim` в игре.

2. **Авто-синхронизация статистики**:
   - **Суть**: Поля профиля (`fish`, `coins`, `playtime_hours`) не обновляются с сервера.
   - **План реализации**: Добавить отправку JSON-пакета статистики на `/api/internal/sync-stats` при выходе игрока с сервера.

---

## 3. 🚀 Лаунчер (AquaTech Launcher & Client Architecture)

### 🔴 Критические проблемы:
1. **Отсутствие сквозной авторизации (Offline Server Vulnerability)**:
   - **Суть**: Сервер работая в `online-mode=false` уязвим к входу под чужим ником (включая админа `Renfild`).
   - **План реализации**: 
     - Лаунчер получает токен `session_token` через `/api/launcher/session`.
     - При подключении отправляется Forge-пакет `C2SAuthPacket(nick, session_token)`.
     - Сервер сверяет токен с D1. В случае несоответствия — кик игрока.

2. **Отсутствие интерактивного Парсера Крашей**:
   - **Суть**: Вылет Forge или JVM закрывает лаунчер без разбора причин.
   - **План реализации**: Реализовать в `launcher_bridge.py` анализ `hs_err_pid*.log` и `latest.log` с выводом понятного решения на русском языке и кнопкой отправки отчета в Discord.

### 🟡 Оптимизации лаунчера:
1. **Динамический RAM Allocator & JVM ZGC / G1GC**:
   - **Суть**: Фиксированные 2-16 ГБ RAM вызывают лаги GC или OOM.
   - **План реализации**: Динамическая рекомендация 50% свободной памяти ПК (4-8 ГБ) + автоматическое подставление параметров `-XX:+UseG1GC -XX:MaxGCPauseMillis=50`.

2. **Кнопка «Восстановление и очистка сборки»**:
   - **Суть**: Поврежденные файлы не восстанавливаются при стандартной проверке.
   - **План реализации**: Функция полной валидации хешей MD5 всех модов/конфигов и удаления мусорных `.tmp` файлов.

---

## 4. 🛠️ Инфраструктура, Мониторинг & DevOps

### 🔴 Критические проблемы:
1. **Отсутствие внешних бэкапов в Cloudflare R2**:
   - **Суть**: Бэкапы хранятся локально на Apex Nodes.
   - **План реализации**: Cron-скрипт отправки зашифрованного архива `world/` в Cloudflare R2 каждые 6 часов.

2. **TPS Watchdog & Очистка выроненного лута**:
   - **Суть**: Переполнение `AutoFisher` приводит к просадкам TPS.
   - **План реализации**: Мониторинг TPS через Spark API + авто-очистка выроненного лута старше 3 минут при TPS < 13.

3. **Сквозные Smoke-тесты в CI/CD**:
   - **Суть**: Отсутствует комплексная проверка онлайн-инфраструктуры.
   - **План реализации**: Расширить `smoke_apex_server.py` проверкой RCON -> Web API -> CDN Manifest -> Discord Webhook.

---

## 📊 Сводная матрица приоритетов (Action Matrix)

| Приоритет | Компонент | Задача | Техническая реализация |
|---|---|---|---|
| 🔥 **P0** | **Лаунчер + Сервер** | Сквозная токен-авторизация (`session_token`) | `C2SAuthPacket` в `aquatech_ui` + D1 проверка |
| 🔥 **P0** | **Сайт + Сервер** | Онлайн-донат и авто-доставка | `/api/purchase/callback` + D1 `pending_commands` |
| ⚡ **P1** | **Сборка** | Защита прочности удочек от Починки/Прочности | Перехват `AnvilUpdateEvent` в `aquatech_ui` |
| ⚡ **P1** | **Сборка** | Защита от дюпов инвентаря на Mohist | `ContainerTransactionEvent` + лимит на `AutoFisher` |
| ⚡ **P1** | **Инфраструктура**| Авто-бэкапы мира в Cloudflare R2 | Cron-скрипт бэкапа мира в R2 bucket |
| 🔹 **P2** | **Сборка** | Мост экономик (Lightman's ↔ CasesMod) | `CurrencyBridgeHandler` (обмен монет на дублоны F4) |
| 🔹 **P2** | **Сайт** | Синхронизация статистики в D1 | Hook `PlayerQuitEvent` -> `/api/internal/sync-stats` |
| 🔹 **P2** | **Лаунчер** | Парсер крашей и динамический RAM | Анализ `hs_err_pid.log` + JVM G1GC флаги |
| 💡 **P3** | **Сборка** | Океанический Престиж и Босс Бездны | KubeJS/Java система престижа и босса |
