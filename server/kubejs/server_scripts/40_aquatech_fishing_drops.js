// AquaTech: resource fishing loot is owned by aquatech_ui FishingLootHandler.
// This script intentionally does NOT add ItemFishedEvent drops (avoids double loot
// and conflicts with Java cancel+awardCatch for StarCatcher resource rods).
// Kept as a stub so older server copies that reference the file stay harmless.

console.info('[AquaTech] 40_aquatech_fishing_drops.js: loot handled by aquatech_ui (no-op)')
