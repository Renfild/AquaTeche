// AquaTech: portal sync moved into the aqualumen mod (ServerEvents periodic push).
// The KubeJS sandbox blocks java.lang.Thread, so HTTP from scripts is impossible.
console.info('[portal-sync] handled by aqualumen mod (no-op)')
