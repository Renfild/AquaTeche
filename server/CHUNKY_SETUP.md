# Chunky — отключён

Forge-мод `Chunky-1.3.146.jar` и Paper-плагин `Chunky.jar` вместе жрали CPU Apex до 100%.

Не ставить обратно. Преген радиуса 3000 уже лежит в region-файлах; `continue-on-restart` не нужен.

Если когда-нибудь снова понадобится точечный преген — один из двух (мод **или** плагин), и только при пустом сервере.
