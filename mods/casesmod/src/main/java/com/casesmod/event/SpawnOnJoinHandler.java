package com.casesmod.event;

/**
 * Disabled: Raft spawner handles player island placement (Y=190).
 * Forceful hub teleport on login causes players to be crushed inside solid atoll blocks.
 */
public class SpawnOnJoinHandler {
    // Intentionally empty. Do not register @Mod.EventBusSubscriber.
}
